package com.focusguard.app.ui.theme

import androidx.compose.ui.graphics.Color

object FgElevation {
    // Legacy alias kept for any remaining usages
    val cardShadowColor = Color(0xFF6366F1).copy(alpha = 0.10f)
    val clayCardAmbient = Color(0xFF6366F1).copy(alpha = 0.15f)
    val clayCardSpot    = Color(0xFF4F46E5).copy(alpha = 0.10f)
    val glassShadow     = Color(0xFF6366F1).copy(alpha = 0.08f)
}
